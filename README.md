# Inverted-Search
The project builds an inverted index that maps each word to the files in which it appears. This approach enables faster and more efficient searching across multiple text files. The project mainly depends on Hashing Technique.
